package com.Questions.Chapter4;

/**
 * Created by hkaunda on 12/13/17.
 */
public class MinimalTree {
    static <T> BinTree<T> minimalTree(T[] sortedArray) {

    }

    static <T> void minimalTree(T[] sortedArray, int start, int end) {
        /* method needs to return a binary tree Node.
         * Going to change up the implementation of BinTree so we can either use the [node] class outside BinTree.
         */
    }
}
